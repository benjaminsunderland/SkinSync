<%= form_with(model: tattoo, local: true) do |form| %>
  <% if tattoo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tattoo.errors.count, "error") %> prohibited this tattoo from being saved:</h2>

      <ul>
      <% tattoo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :tattoo_name %>
  </div>

  <div class="field">
    <%= form.label :image_link %>
    <%= form.text_field :image_link, id: :tattoo_image_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
